Installing flet-cli 0.27.6 package...OK
usage: flet [-h] [--version] {create,run,pack,publish,build,doctor} ...

positional arguments:
  {create,run,pack,publish,build,doctor}
    create              Create a new Flet app from a template.
    run                 Run Flet app.
    pack                Package Flet app to a standalone bundle.
    publish             Publish Flet app as a standalone web app.
    build               Build an executable app or install bundle.
    doctor              `flet doctor` command to provide information about the
                        system and environment setup.

options:
  -h, --help            show this help message and exit
  --version             show program's version number and exit
